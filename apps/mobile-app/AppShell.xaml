<?xml version="1.0" encoding="utf-8" ?>
<Shell
    x:Class="VinhKhanh.MobileApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:VinhKhanh.MobileApp.Pages"
    FlyoutBehavior="Disabled"
    Shell.NavBarIsVisible="False">

    <ShellContent
        Route="LoginPage"
        ContentTemplate="{DataTemplate pages:LoginPage}" />

    <ShellContent
        Route="HomeMapPage"
        ContentTemplate="{DataTemplate pages:HomeMapPage}" />

    <ShellContent
        Route="MyTourPage"
        ContentTemplate="{DataTemplate pages:MyTourPage}" />

    <ShellContent
        Route="QrScannerPage"
        ContentTemplate="{DataTemplate pages:QrScannerPage}" />

    <ShellContent
        Route="SettingsPage"
        ContentTemplate="{DataTemplate pages:SettingsPage}" />

    <ShellContent
        Route="LanguageSelectionPage"
        ContentTemplate="{DataTemplate pages:LanguageSelectionPage}" />
</Shell>
