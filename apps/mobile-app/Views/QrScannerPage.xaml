<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.Maui.Controls"
             xmlns:controls="clr-namespace:VinhKhanh.MobileApp.Controls"
             x:Class="VinhKhanh.MobileApp.QrScannerPage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#151F33">
    <Grid RowDefinitions="*,Auto">
        <Grid Grid.Row="0">
            <zxing:CameraBarcodeReaderView x:Name="BarcodeReader"
                                           BarcodesDetected="OnBarcodesDetected"
                                           Opacity="0.25" />
            <BoxView Color="#151F33" Opacity="0.88" />

            <Grid Padding="18,22,18,104" RowDefinitions="Auto,*,Auto">
                <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="12">
                    <Border WidthRequest="46" HeightRequest="46" BackgroundColor="#2A3447" StrokeThickness="0" StrokeShape="RoundRectangle 18">
                        <Label Text="✕" HorizontalOptions="Center" VerticalOptions="Center" FontSize="20" TextColor="White" />
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnCloseTapped" />
                        </Border.GestureRecognizers>
                    </Border>
                    <Grid Grid.Column="2">
                        <Border WidthRequest="46" HeightRequest="46" BackgroundColor="#2A3447" StrokeThickness="0" StrokeShape="RoundRectangle 18">
                            <Label Text="⚡" HorizontalOptions="Center" VerticalOptions="Center" FontSize="18" TextColor="White" />
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnTorchTapped" />
                            </Border.GestureRecognizers>
                        </Border>
                    </Grid>
                    <Grid Grid.Column="3">
                        <Border WidthRequest="46" HeightRequest="46" BackgroundColor="#2A3447" StrokeThickness="0" StrokeShape="RoundRectangle 18">
                            <Label Text="⌘" HorizontalOptions="Center" VerticalOptions="Center" FontSize="18" TextColor="White" />
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnManualTapped" />
                            </Border.GestureRecognizers>
                        </Border>
                    </Grid>
                </Grid>

                <Grid Grid.Row="1" HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="250" HeightRequest="250">
                    <BoxView WidthRequest="40" HeightRequest="3" Color="{StaticResource BrandOrange}" HorizontalOptions="Start" VerticalOptions="Start" Margin="0,0,0,0" />
                    <BoxView WidthRequest="3" HeightRequest="40" Color="{StaticResource BrandOrange}" HorizontalOptions="Start" VerticalOptions="Start" Margin="0,0,0,0" />
                    <BoxView WidthRequest="40" HeightRequest="3" Color="{StaticResource BrandOrange}" HorizontalOptions="End" VerticalOptions="Start" />
                    <BoxView WidthRequest="3" HeightRequest="40" Color="{StaticResource BrandOrange}" HorizontalOptions="End" VerticalOptions="Start" />
                    <BoxView WidthRequest="40" HeightRequest="3" Color="{StaticResource BrandOrange}" HorizontalOptions="Start" VerticalOptions="End" />
                    <BoxView WidthRequest="3" HeightRequest="40" Color="{StaticResource BrandOrange}" HorizontalOptions="Start" VerticalOptions="End" />
                    <BoxView WidthRequest="40" HeightRequest="3" Color="{StaticResource BrandOrange}" HorizontalOptions="End" VerticalOptions="End" />
                    <BoxView WidthRequest="3" HeightRequest="40" Color="{StaticResource BrandOrange}" HorizontalOptions="End" VerticalOptions="End" />
                    <BoxView HeightRequest="2" WidthRequest="210" Color="{StaticResource BrandOrange}" HorizontalOptions="Center" VerticalOptions="Center" />
                </Grid>

                <VerticalStackLayout Grid.Row="2" Spacing="14">
                    <Label Text="Quét QR" FontSize="24" FontAttributes="Bold" TextColor="White" HorizontalTextAlignment="Center" />
                    <Label Text="Vui lòng đưa mã QR vào trong khung để nghe giới thiệu chi tiết."
                           FontSize="14"
                           TextColor="#D0D7E7"
                           HorizontalTextAlignment="Center" />
                    <Border Padding="16,14" BackgroundColor="#333E53" Stroke="#4D586E" StrokeShape="RoundRectangle 18">
                        <HorizontalStackLayout Spacing="12">
                            <Border WidthRequest="36" HeightRequest="36" BackgroundColor="#FF5A0A33" StrokeThickness="0" StrokeShape="RoundRectangle 14">
                                <Label Text="⌘" HorizontalOptions="Center" VerticalOptions="Center" FontSize="16" TextColor="{StaticResource BrandOrange}" />
                            </Border>
                            <Label Text="Quét mã QR tại bàn hoặc trước cửa quán để nghe giới thiệu chi tiết."
                                   FontSize="13"
                                   TextColor="#E6EBF5"
                                   VerticalTextAlignment="Center" />
                        </HorizontalStackLayout>
                    </Border>
                </VerticalStackLayout>
            </Grid>
        </Grid>

        <controls:AppBottomBar Grid.Row="1" CurrentRoute="QrScannerPage" />
    </Grid>
</ContentPage>
