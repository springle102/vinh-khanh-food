VITE_APP_NAME=Vinh Khanh Admin
VITE_PORT=5173
VITE_PREVIEW_PORT=4173
VITE_API_BASE_URL=/api/v1
# Optional override when backend dev runs on a custom URL.
# Leave empty to auto-detect from ASPNETCORE_URLS / VK_BACKEND_URLS / backend launchSettings.
VITE_DEV_API_TARGET=
